// Phase A scaffold shell only — no app logic yet.
// Features are built test-first in Phase B, one signed-off spec at a time.
export default function App() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center gap-2 text-center p-8">
      <h1 className="text-3xl font-bold text-trail">Trailward</h1>
      <p className="text-gray-600">Toward the trail — scaffold ready. Specs are under review.</p>
    </main>
  );
}
