# Trailward needs NO environment variables to build or run: data is baked into
# the repo, and feedback goes through GitHub Issues (spec 29) — prefilled links,
# no keys. This file exists only to document that fact.
