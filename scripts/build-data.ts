/**
 * build-data — regenerates src/data/treks.json from free public sources
 * (Overpass, Open-Meteo, OSRM, Wikipedia/Commons) + build-time scraping.
 *
 * Phase A: STUB. The real multi-source pipeline is implemented test-first in
 * Phase B per specs/02-data-pipeline.md. This stub keeps `npm run build:data`
 * runnable and the CI wiring honest.
 */
console.log("[build-data] stub — pipeline implemented in Phase B (see specs/02-data-pipeline.md)");
